// Named function
function add(x: number, y: number): number {
	return x + y;
}
add(1, 2);

// Anonymous function
let myAdd = function (x: number, y: number): number {
	return x + y;
};
myAdd(3, 4);

export {};
